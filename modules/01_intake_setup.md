# Module 1: Intake & Setup

## Responsibilities
- Normalize section names.
- Detect missing sections:
  - If section not found → label as **missing**.
- Count words per section:
  - If <50 words → label as **short**.
  - Otherwise → no label.

## Outputs
- Section list with labels (missing/short/normal).
- Word counts per section.
